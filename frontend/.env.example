# Optional API key header for deployments that set SCC_API_KEYS/API_KEYS on the FastAPI backend.
VITE_API_KEY=

# Optional frontend fallback. The UI normally reads CHAT_USER_ID through GET /api/chat/defaults.
VITE_DEFAULT_USER_ID=
